# my-mastra-product

我正在构建一个网页端AI 产品（类似 workbuddy），项目名叫 my-mastra-product，使用 [typescript] 实现。

## 语言
中文回答，中文注释。

## 测试

开发完功能后，用 tmux 做端到端测试：

开发完功能后，用 tmux 做端到端测试：
1. 在 tmux 会话中启动 my‑mastra‑product Mastra后端服务，确认服务正常监听端口，无启动、LLM 连接报错。
2. 在 tmux 另一窗格，发送真实业务对话请求，包含普通闲聊、可触发工具调用的指令、多轮上下文对话，复用 threadId 测试会话记忆。
3. 观察服务运行日志，确认 Agent 是否正确判断并调用工具、工具参数正确、工具执行完成、LLM 能够流式生成回复；重点排查404、鉴权、工具执行失败、流式中断等异常。
4. 后端接口验证通过后，再访问网页前端做完整联调；全部场景执行完成，对照 checklist.md 逐项验收。