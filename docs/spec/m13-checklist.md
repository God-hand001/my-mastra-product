# M13 产物文件卡与系统操作菜单 Checklist

> 每项通过运行代码或观察行为验证。主 agent 执行，先有证据再下结论。

## 编译与构建

- [ ] 后端编译无错误（验证：根目录 `npx tsc --noEmit` 退出码 0）
- [ ] 前端编译与构建无错误（验证：`cd app && npm run build` 成功）

## 卡片样式

- [ ] 长卡布局与图标（验证：AC1——生成 Word 与 PDF 产物，两卡均为长卡，图标蓝/红可辨，副标题 `DOCX · N KB` / `PDF · N KB` 正确）
- [ ] 多产物样式一致（验证：一条消息含 2+ 产物时各占一卡、样式相同、脚本类产物折叠区内的卡样式也一致）
- [ ] 点击主体预览不受影响（验证：AC2——点卡片主体进站内预览；点三点只开菜单不进预览）
- [ ] 大小懒加载降级（验证：断网/后端停时打开页面，卡片正常显示只有类型无大小，不报错不空白）

## 三点菜单

- [ ] 菜单结构与开关（验证：AC3——五入口齐全；点外部/Esc 关闭；反复开关正常）
- [ ] 打开（验证：AC4——.docx 点「打开」由系统默认程序打开）
- [ ] 打开文件夹（验证：AC5——资源管理器打开产物目录且文件选中）
- [ ] 另存为（验证：AC6——触发标准下载，保存后文件完整可开）
- [ ] 复制路径（验证：AC7——粘贴得到本机绝对路径，按钮出现「已复制」反馈）
- [ ] 打开方式子菜单（验证：AC8——pdf/docx 各列出关联应用、默认带标记、图标显示（缺图标降级通用）；点非默认应用成功打开）
- [ ] 空关联降级（验证：AC9——自造扩展名产物子菜单显示「没有可用的应用」）
- [ ] 失败提示（验证：AC11——删产物文件后点「打开」菜单内出现中文错误提示，无崩溃）

## 后端与安全

- [ ] stat 端点（验证：真实产物返回正确 size 与绝对路径；不存在返回 404 中文原因）
- [ ] 越界拒绝（验证：AC10——path 传 `..\\..\\.env` 类构造值，stat/open/openwith 全部 400/404，无任何文件操作发生）
- [ ] appId 白名单（验证：伪造 appId 调 open-with 返回 400；枚举缓存中的真实 appId 正常执行）
- [ ] 枚举缓存（验证：AC12——同扩展名第二次开子菜单明显快于第一次；服务端日志确认第二次未重跑 PowerShell）
- [ ] PowerShell 脚本健壮性（验证：对 .pdf/.docx/.txt/.xyz13 四种扩展名各跑一次，输出合法 JSON，单条应用失败不影响整体）

## 非功能

- [ ] 文本注入安全（验证：AC13——产物名含 `<b>x</b>` 时卡片与菜单按纯文本显示；代码走查无 dangerouslySetInnerHTML）
- [ ] 键盘导航（验证：AC14——Tab 到三点按钮 → Enter 开菜单 → ↑↓ 移动 → Enter 执行 → Esc 关闭全程可达）
- [ ] 视觉一致（验证：菜单浮层色板/圆角/阴影与现有模型选择器菜单一致，目视）
- [ ] 旧样式清理（验证：全局 grep `artifact-card` 无残留引用，旧 CSS 类已删）

## 内联预览

- [ ] pptx 内联渲染（验证：AC15——生成 pptx 产物，卡内直接可见幻灯片画面、纵向翻页滚动，无需进预览栏）
- [ ] html 内联渲染（验证：AC16——生成 html 产物，卡内渲染网页画面、内部滚动；iframe 无 allow-same-origin，代码走查确认）
- [ ] 预览区联动预览栏（验证：AC17——点内联预览区右侧预览栏打开该产物；三点菜单五项照常）
- [ ] 内联失败降级（验证：AC18——损坏 pptx 的预览区显示失败提示，卡片与菜单正常）

## 端到端场景

- [ ] 场景 1（全流程）：生成带 Word 附件的任务 → 长卡显示 → 三点菜单逐项真实执行（打开/定位/另存/复制/指定应用打开）全部成功
- [ ] 场景 2（异常路径）：删除产物后操作 → 错误提示可见 → 重新生成任务后新卡功能正常
- [ ] 场景 3（历史回看）：刷新页面 → 历史消息产物卡长卡样式与菜单功能与实时一致
