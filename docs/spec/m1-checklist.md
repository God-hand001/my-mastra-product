# M1 联网搜索 Checklist

> 每一项通过运行代码或观察行为来验证。括号内为验证方式;[ACx] 对应 spec 验收标准。

## 实现完整性

- [ ] web_search 工具返回编号结果与来源小节(验证:T1 自测:中文 query → results 含 index/title/url/snippet + sourceListMarkdown)
- [ ] agent 自主发起搜索,无需用户提示(验证:发"查一下 JLCPCB 是什么公司",[AC1])
- [ ] 搜索过程经工具卡片可见:搜索词、运行中→完成(验证:观察工具卡片,[AC1])
- [ ] 回答正文有 `[1]` `[2]` 内嵌标注(验证:[AC2])
- [ ] 末尾"来源"小节:编号+标题+URL 与正文标注一一对应(验证:[AC2])
- [ ] 纯本地任务不出现来源小节、不滥用搜索(验证:发"看看 workspace 里有什么文件",[AC3])
- [ ] 中文搜索词正常可用(验证:发"嘉立创最近的新闻",[AC4])
- [ ] 搜索失败不阻塞任务(验证:临时改错 key → agent 说明搜索受阻并正常收尾;测后还原,[AC6])

## 集成

- [ ] 供应商隔离:Tavily 字样只存在于 web-search-tool.ts(验证:grep src/ 与 app/,[AC5])
- [ ] `.env.example` 只有占位符,无真实 key(验证:读文件)
- [ ] 前端零改动(验证:M1 提交范围内 app/ 目录无 diff)
- [ ] M0 功能无回归:建任务、工具卡片、历史持久化抽查正常(验证:抽查一轮任务)

## 编译与质量

- [ ] 后端启动无报错(验证:npm run dev 日志)
- [ ] 前端 tsc 通过(验证:app/ 下 tsc --noEmit;预期无代码改动)
- [ ] 无密钥泄漏(验证:git log -p 与工作区中 grep 真实 key 前缀)

## 端到端场景

- [ ] 场景 1(主流程):新任务 → 自动搜索 → 工具卡片 → `[1][2]` 标注 → 来源列表,全程无报错
- [ ] 场景 2(中文):中文时事查询,搜索词与结果均可用
- [ ] 场景 3(边界):本地任务不触发搜索
- [ ] 场景 4(故障):错 key → 优雅失败,还原后复测正常
