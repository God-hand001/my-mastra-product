# M12 执行过程可观测化 验收报告

> 2026-09-18 定稿。**验收结论:通过(用户确认)**。

## 自动化验证结果

### 编译与构建
- [x] 根 `npx tsc --noEmit` 退出码 0
- [x] 前端 `npm run build`(tsc + vite)成功
- [x] `diff@8.0.4` 显式声明,`diffLines` 可用

### 后端 diff 数据(真实链路)
- [x] 新建文件:`result.diff = {isNew:true, hunks:[add…], added:N}` —— 真实 curl 调用验证
- [x] 覆盖文件:del/add 成对 + ctx 保留,+1 -1 计数正确 —— 真实 curl 调用验证
- [x] 断言套件 6 组全过(新建/修改/60k 截断/相同内容/空串覆盖/400 行边界)
- [x] 降级分支走查(读旧内容失败 → truncated:true 不阻断写入)

### 思考流式(用户反馈修复后)
- [x] `chatRoute sendReasoning:true` 实测生效:reasoning-start ×1 + delta ×72 + end ×1(此前 0)
- [x] ReasoningBlock 流式自动滚底;修复 hooks 顺序崩溃 bug(条件 return 后调 hooks)

### 工具卡与状态行
- [x] toolMeta 语义文案对齐千问 operations(创建文件/查阅文件/查看列表/规划步骤等)
- [x] task_write/update/complete/check 待办工具族语义卡,摘要行不露 JSON
- [x] StepsFold 流式摘要行显示实时阶段词,完成后显示"任务已完成"
- [x] 状态行与卡片文案同源(computePhaseLabel + toolMeta)

### 用户反馈修复(2026-09-18 下午,7 项)
1. 流式中折叠头显示阶段词(原固定"任务已完成")—— StepsFold label 流式分支
2. 检索网页自动打开侧边栏 —— TaskPage 边沿检测改为消息 id 去重(重挂载不丢)
3. 深度思考卡自动滚到最新输出 —— reasoning-body 流式 scrollTop 同步
4. 侧边栏网页只显示一半 —— webview 高度链修复(absolute 铺满)+ 溢出检测缩放(响应式页 1:1)
5. 过程输出语义化 —— toolMeta 对齐千问 operations 文案;待办工具不露 JSON
6. 标签页右键菜单(关闭/关闭其他/关闭右侧)—— 原生事件委托 + createPortal(document.body)
7. 嵌套 details toggle 冒泡 bug —— 点内层思考卡收起会连带收起外层,e.target !== currentTarget 修复

### 浏览器验收(用户执行)
- [x] 场景 1-4(多步过程/文件 diff/异常路径/历史回看) + 思考流式 + 来源徽章 + webview 适配 + 右键菜单 —— 用户逐项确认通过("ok,通过")

## 遗留
- 视觉细节(间距/颜色)以用户反馈微调
- `list` 规则可能误命中含 "list" 的连接器工具名(兜底可接受)
